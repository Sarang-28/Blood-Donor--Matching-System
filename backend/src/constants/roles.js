const ROLES = {
    DONOR: 'donor',
    HOSPITAL: 'hospital',
    PATIENT: 'patient',
    NGO: 'ngo',
    ADMIN: 'admin',
};

const ALL_ROLES = Object.values(ROLES);

module.exports = {
    ROLES,
    ALL_ROLES,
};
